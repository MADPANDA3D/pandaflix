name: Release

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/debian.yml"
      - "packaging/debian/generate-data.sh"
      - "workflow-trigger"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write

jobs:
  lesbian:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install lesbian build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential debhelper dh-golang just libsixel-bin chafa fzf golang yt-dlp ffmpeg upx-ucl

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: generate lesbian data
        run: bash ./packaging/debian/generate-data.sh

      - name: prepare lesbian directory
        run: cp -r packaging/debian debian

      - name: build lesbian package
        run: |
          export DEB_BUILD_OPTIONS="nocheck"
          dpkg-buildpackage -us -uc -b
          cp ../*.deb pandaflix-1.2.0.deb

      - name: upload lesbian package
        uses: actions/upload-artifact@v4
        with:
          name: pandaflix-deb
          path: pandaflix-1.2.0.deb


